apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kubemate-postgres-pvc
  labels:
    app: kubemate-postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
