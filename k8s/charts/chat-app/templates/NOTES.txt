{{- /*
Post-install notes for chat-app
*/ -}}

🎉 Your Kubemate chat app is deployed in the "kubemate" namespace!

🔐 Default Admin Credentials (CHANGE IMMEDIATELY!):
- Username: admin
- Password: admin123

⚠️  IMPORTANT: Login immediately and change the default admin password!

Access the application:

Port-forward to access frontend locally:
  kubectl port-forward svc/kubemate-frontend 3000:80 -n kubemate

Then open your browser to: http://localhost:3000

Check deployment status:
  kubectl get pods -n kubemate
  kubectl get svc -n kubemate

View application logs:
  # Backend logs
  kubectl logs -f deployment/kubemate-backend -n kubemate

  # Frontend logs
  kubectl logs -f deployment/kubemate-frontend -n kubemate

  # Database logs
  kubectl logs -f deployment/kubemate-postgres -n kubemate